#include <iostream>

using namespace std;

int main() {
    int len;
    cin>>len;

    char* line = new char[50];
    char* head = new char[50];
    char* temp = new char[50];

    for(int i=0;i<len;i++){
        cin.getline(line,50);


    }

}